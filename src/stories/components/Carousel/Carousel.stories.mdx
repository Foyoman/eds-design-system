import Carousel from '../../../components/Carousel/Carousel.tsx';
import Card from '../../../components/Card/Card.tsx';
import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'

<Meta
	title="Carousel/Carousel"
	component={Carousel}
	argTypes={{
		// ...
	}}
/>

# Carousel

A Carousel

export const CarouselTemplate = (args, { argTypes }) => (
	<Carousel {...args}>
		<Card />
		<Card />
		<Card />
		<Card />
	</Carousel>
)

<Canvas>
	<Story
		name="Overview"
		parameters={{
			docs: {
				source: {
					type: "dynamic",
					format: true,
				},
			},
	  }}
		args={{
			heading: 'Heading',
			subheading: 'Subheading',
			items: [
				{
					name: 'Card',
					title: 'yes',
					link: {
						linktype: 'external',
						url: 'https://www.edwardtdo.com'
					}
				}
			]
		}}
  >
		{CarouselTemplate.bind({})}
	</Story>
</Canvas>

### Props & Slots

<ArgsTable story="Overview" />