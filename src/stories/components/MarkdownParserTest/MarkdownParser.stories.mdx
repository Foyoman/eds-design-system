import MarkdownParser from '../../../components/MarkdownParserTest/MarkdownParserTest.tsx';
import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'

<Meta
	title="MarkdownParserTest/MarkdownParserTest"
	component={MarkdownParser}
	argTypes={{
		// ...
	}}
/>

# MarkdownParser

A MarkdownParser

export const MarkdownParserTemplate = (args, { argTypes }) => <MarkdownParser {...args} />

<Canvas>
	<Story
		name="Overview"
		parameters={{
			docs: {
				source: {
					type: "dynamic",
					format: true,
				},
			},
	  }}
		args={{
			content: `# this is a test \ni love writing my notes in markdown with syntax highlighted code block integration so i made this wonderful component \n\n---\n\`\`\`tsx \nconst hello = 123\n\`\`\``,
			theme: 'dark'
		}}
  >
		{MarkdownParserTemplate.bind({})}
	</Story>
</Canvas>

### Props & Slots

<ArgsTable story="Overview" />